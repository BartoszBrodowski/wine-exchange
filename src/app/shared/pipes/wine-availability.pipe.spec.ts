import { WineAvailabilityPipe } from './wine-availability.pipe';

describe('WineAvailabilityPipe', () => {
  it('create an instance', () => {
    const pipe = new WineAvailabilityPipe();
    expect(pipe).toBeTruthy();
  });
});
